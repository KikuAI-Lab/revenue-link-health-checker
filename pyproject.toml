[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "revenue-link-health"
version = "0.1.0"
description = "Local-first revenue-link health checker and QA report workflow"
readme = "README.md"
requires-python = ">=3.11"

[project.scripts]
linkhealth = "linkhealth.cli:main"
